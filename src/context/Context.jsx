import React from "react";

export const ModalBoxContext = React.createContext();
export const UserContext = React.createContext();
export const AppContext = React.createContext()


export default(
    ModalBoxContext,
    UserContext,
    AppContext

)